import numpy as np
import random

z = np.arange(2,10)
print(z)

# Find the exponential applied element-wise to z
print(np.exp(z))

# Find the square root applied element-wise to z
print(np.sqrt(z))


# Find the square applied element-wise to Z.
print(np.square(z))

X = np.random.rand((3,3))
print(X)